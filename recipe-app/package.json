{
  "name": "recipe-capture",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AI recipe box — capture recipes from any URL (blogs, Instagram, TikTok, YouTube) or pasted text",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.112.0",
    "express": "^4.21.2"
  }
}
